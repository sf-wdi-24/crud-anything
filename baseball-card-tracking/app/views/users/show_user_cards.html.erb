<% @card_groupings.each do |search, values| %>
	<b><%= "Search: #{search}" %></b><br>
	<% values.sort_by{|v| v.price}.each do |v| %>
		<li><%= "Sold for $#{v.price} on #{v.endTime} after #{v.numberBids || 0} bids - " %><%= link_to v.title, v.url %></li>
	<% end %>
	<br>
<% end %>